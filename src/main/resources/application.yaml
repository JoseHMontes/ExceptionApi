spring:
  application:
    name: jose
  datasource:
    url: jdbc:mysql://localhost:3306/jose_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: jose
    password: jose
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
